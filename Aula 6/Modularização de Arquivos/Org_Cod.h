#ifndef MAPA_H
#define MAPA_H

// Protótipos das funções solicitadas [cite: 211]
int obter_altura();        // Retorna a altura do mapa [cite: 212]
int obter_largura();       // Retorna a largura do mapa [cite: 213]
int obter_total_celulas(); // Retorna o número de células [cite: 214]
char** obter_matriz();     // Retorna a matriz do mapa [cite: 215]

#endif
