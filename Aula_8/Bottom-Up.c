/* Programa para registro de venda
 * Elementos a serem implementados:
 * Implementar o tipo de entrada desejada (SWITCH)
 * Registro de notas.
 * - log de eventos.
 * Adição de vendas.
 * Controle de Estoque.
 * - gerar os dados do produto.
 */


#include <stdio.h>

struct produtos{
	int id;
	int nome;
	int valor;
	int quantidade;
};
struct notas{

};

//protótipos
int criar_produto(char nome, int valor, int quantidade);
int criar_nota(); //quais as informações relacionadas.
'int criar_venda();
int registrar_valor();
int registrar_id();
int registrar_produto();
int registrar_quantidade();
int registrar_venda();
int gerar_nota();
int calcular_valores();

struct node{
	int prox*;
	int 
	int node*;
}

arquivo array



int main (){

	int opcao;
	int nome;
	int valor;
	int ID;
	
	scanf("Escreva a opção desejada%d\n", &opcao);
	if (opcao = 1)
	{
		scanf("Escreva o nome do produto %s\n",&nome);
		getchar();
		scanf("Escreva o preço do produto %d\n",&valor);
		scanf("Escreva a quantidade do produto%d\n",&quantidade);
		scanf("Escreva o ID do produto %d\n", &iID);
		criar_produto(nome, valor, quantidade);		
	}
	if (opcao = 2)
	{
		criar_venda()
	}
	if (opcao = 3)

	criar produto(nome, valor, quantidade);
	
	





	return 0;
}
