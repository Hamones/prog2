
//implementação das aulas de strings

#include<stdio.h>
#include<stdlib.h>

int main(){
int i;
//imprimir hello world
char palavra[20] = {"Hello World!"};
// "" adiciona '\0' ao vetor.


printf("%s", palavra);

char palavra[15] = 'x';

for (i = 0; i < 20; i++)
{
  if (palavra[i] != '\0'); //impressão até o character /0
    printf("%c",palavra[i]);
  else
    break;
}
//funções

char palavra[100] = {"Abacate"};
char *letra;

letra = strchr(palavra,'z');

//verificar se existe esse vetor
if (letra)
{
	//imprimir o conteúdo do vetor.
	printf("%c\n",*letra);
}

//procurar uma palavra;

char frase[100]={"Casa branca senzala"};
char palavra[100]={"branca"};

ponteiro = strstr(frase,palavra); //associa duas estrings e retorna o endereço

//converter para maiúsculo e minúsculo;

<ctype.h> //alterar caracter.

char letra = 'c';
char ma, mi;

ma = toupper(letra);
mi = tolower(ma);

printf("n\t\ %c/ n\t\",ma, mi);
