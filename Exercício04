#include <stdio.h>
#include <string.h>
#define TAM 1000

struct node
{
 	char palavra[100];
	int frequencia;
};

int main ()
{

	struct palavra texto[TAM];
	int i = 0;


	while (stream)
	{
	
		texto [i] = fget(file; sdtin);
		
		if texto [i] = '\n'
			i++;	
	}
	i = 0;

	for (i; i < TAM; i++)
	{
		for (j ; j < TAM; j++)
			if texto[i]->palavra = texto [j]->palavra;
			texto[i]->frequencia++;	
	}

	return 0;
}